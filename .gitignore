# Local Claude settings may hold secrets — never commit that file.
# Commands/skills under .claude ARE shared (they must be in the repo so any
# cloud session can run them, e.g. /consult), so keep them tracked.
.claude/settings.local.json
.claude/settings.json

# Other local projects in this folder - separate repos
NAC-PROPERTY-HUB.html
NAC-RESIDENCE-INDEX.html
Brochures html/

# OS files
.DS_Store
Thumbs.db

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
